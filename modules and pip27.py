#modules are the python files that can be imported into the current python file
# external libraries--lib has all the external files in the folder           #lib has site packages file where the downloaded external modules lie
# #modules are external python file

import useful_tools      #importing a file called useful_tools

print(useful_tools .roll_dice(10))     #rolling a 10 sided dice

#pip is a program
#we use pip to install python modules
#pip is reffered to as a  package manager which allows you to manage, install, uninstall and update different python modules